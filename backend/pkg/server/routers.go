package server

import (
	// ".../backend/pkg/controllers"
	// "github.com/gofiber/swagger" // swagger handler
)

// func SetupRouters(x) {
	// ...Routers := x.Group("/api/...")
	// {
	// 	...Routers.Post("/1", controllers.SignUp)
	// 	...Routers.Post("/2", controllers.Login)
	// }

	// x.Get("/swagger/*", swagger.HandlerDefault)
// }
