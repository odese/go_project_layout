package server

import (
	// ".../backend/pkg/infrastructure/config"
)

func Trigger() {
	// x := blabla
	// SetupRouters(x)

	// RunServer(x)
}

// func RunServer(x) {
// 	domain := config.Call().GetString("host.domain")
// 	port := config.Call().GetString("host.port")
// 	x.Listen(domain + port)
// }
